<%- include('partials/header'); -%>

<h1>Home</h1>
<p><%= homeStartingContent %></p>


<% posts.forEach(i => { %>
    <h1> <%= i.title %> </h1>
    
    <% const length =100 %>
    <% let myString = i.article %>
    <% let myTruncatedString = myString.substring(0,length) + "..." %> 
    
    <p> <%= myTruncatedString %> 
        <a href ="/posts/<%= i.title %>">Read More</a>
    </p>
    <% }) %>
    
    
    
    
    
    
    
    <%- include( 'partials/footer'); -%>
  